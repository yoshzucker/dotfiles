# zsh plugins sourced directly by config/shell/zsh/zsh.sh (no plugin manager).
# One git repository URL per line. Inline `#` comments and blank lines are ignored.
#
# Cloned into $XDG_DATA_HOME/zsh/plugins/<repo-name> by:
#   - bootstrap        : install_zsh_plugins()   (reads this file directly)
#   - bootstrap.ps1    : Install-ZshPlugins       (delegates to MSYS2 bash)
# Idempotent: missing -> shallow clone, present -> fast-forward pull.
#
# NOTE: load ORDER is significant and is defined in zsh.sh, not here:
#   zsh-autocomplete (before compinit) -> zsh-autosuggestions.

https://github.com/marlonrichert/zsh-autocomplete
https://github.com/zsh-users/zsh-autosuggestions
